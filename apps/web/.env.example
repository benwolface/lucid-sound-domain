# Where the frontend should send API requests.
# Local dev keeps using Vite's /api proxy, while production should point at
# the separate Vercel API project URL, e.g. https://lucid-sound-domain-api.vercel.app/api
VITE_API_BASE_URL="/api"
